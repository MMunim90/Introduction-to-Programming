#include<stdio.h>
#include<string.h>
int main()
{
    char a;
    scanf("%s", a);
    char b;
    scanf("%s", b);

    int length = strlen(b);

    int x,y;
    scanf("%d %d", &x, &y);

    for(int i=0; i<length; i++)
    {
        
    }
    return 0;
}